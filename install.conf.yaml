- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.zsh: zsh/
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.tmuxline-snapshot: tmuxline-snapshot
    ~/.config: config/

- shell:
  - [git submodule update --init --recursive, Installing submodules]

